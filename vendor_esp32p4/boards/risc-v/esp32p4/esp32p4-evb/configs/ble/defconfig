#
# This file is autogenerated: PLEASE DO NOT EDIT IT.
#
# You can use "make menuconfig" to make any modifications to the installed .config file.
# You can then do "make savedefconfig" to generate a new defconfig file that includes your
# modifications.
#
# ESP32-P4 EVB - BLE (NimBLE) Configuration
#
# This defconfig provides a BLE-enabled NuttX system for the ESP32-P4 EV Board with:
#   - RISC-V architecture (RV32IMAFC)
#   - UART0 serial console at 115200 baud
#   - GPIO and Timer drivers
#   - 768KB internal SRAM + 32MB external PSRAM
#   - NimBLE BLE stack with GATT server (custom service + Heart Rate Service)
#   - NSH with readline and builtin apps
#
# IMPORTANT: ESP32-P4 has NO built-in Bluetooth radio. An external BLE
# co-processor (e.g., ESP32-C6 or ESP32-H2) connected via HCI UART is
# required for BLE functionality. The ESP-IDF bt component must be
# integrated into the build system.
#

#
# Architecture
#
CONFIG_ARCH="risc-v"
CONFIG_ARCH_BOARD="esp32p4-evb"
CONFIG_ARCH_BOARD_CUSTOM=y
CONFIG_ARCH_BOARD_CUSTOM_DIR="../vendor_esp32p4/boards/risc-v/esp32p4/esp32p4-evb"
CONFIG_ARCH_BOARD_CUSTOM_DIR_RELPATH=y
CONFIG_ARCH_BOARD_CUSTOM_NAME="esp32p4-evb"
CONFIG_ARCH_CHIP="esp32p4"
CONFIG_ARCH_CHIP_CUSTOM=y
CONFIG_ARCH_CHIP_CUSTOM_DIR="../vendor_esp32p4/chips/esp32p4"
CONFIG_ARCH_CHIP_CUSTOM_DIR_RELPATH=y
CONFIG_ARCH_CHIP_CUSTOM_NAME="esp32p4"
CONFIG_ARCH_CHIP_ESP32P4=y
CONFIG_ARCH_RISCV=y
CONFIG_ARCH_INTERRUPTSTACK=2048
CONFIG_ARCH_STACKDUMP=y

#
# Board
#
CONFIG_BOARD_LATE_INITIALIZE=y
CONFIG_BOARD_LOOPSPERMSEC=16717
CONFIG_BOARDCTL=y

#
# Scheduler
#
CONFIG_SCHED_WAITPID=y

#
# Memory - 768KB internal SRAM + 32MB external PSRAM
#
CONFIG_RAM_START=0x4FF00000
CONFIG_RAM_SIZE=786432
CONFIG_MM_REGIONS=2
CONFIG_MM_DEFAULT_ALIGNMENT=16

#
# ESP32-P4 Chip Configuration
#
CONFIG_ESP32P4_FLASH_SIZE=16
CONFIG_ESP32P4_LTO=y

#
# ESP32-P4 UART - Serial Console
#
CONFIG_ESP32P4_UART0=y
CONFIG_ESP32P4_UART0_SERIAL_CONSOLE=y
CONFIG_ESP32P4_UART1=n

#
# ESP32-P4 GPIO
#
CONFIG_ESP32P4_GPIO=y

#
# ESP32-P4 Timer
#
CONFIG_ESP32P4_TIMER=y

#
# ESP32-P4 PSRAM - 32MB OPI PSRAM (BLE requires extra memory)
#
CONFIG_ESP32P4_PSRAM=y
CONFIG_ESP32P4_PSRAM_SIZE=32

#
# ESP-IDF Integration (required for BLE component)
#
CONFIG_ESP_IDF=y

#
# ESP32-P4 BLE Configuration
#
CONFIG_ESP32P4_BLE=y
CONFIG_ESP32P4_BLE_NIMBLE=y
CONFIG_ESP32P4_BLE_DEVICE_NAME="ESP32P4_BLE"
CONFIG_ESP32P4_BLE_ADV_INT_MIN=32
CONFIG_ESP32P4_BLE_ADV_INT_MAX=64
CONFIG_ESP32P4_BLE_MTU=256

#
# NSH Configuration
#
CONFIG_NSH_ARCHINIT=y
CONFIG_NSH_BUILTIN_APPS=y
CONFIG_NSH_FILEIOSIZE=512
CONFIG_NSH_READLINE=y
CONFIG_SYSTEM_NSH=y
CONFIG_INIT_ENTRYPOINT="nsh_main"

#
# Stack Sizes - BLE host task needs more stack
#
CONFIG_IDLETHREAD_STACKSIZE=2048
CONFIG_INIT_STACKSIZE=8192
CONFIG_DEFAULT_TASK_STACKSIZE=4096

#
# Builtin Applications
#
CONFIG_BUILTIN=y

#
# Debug
#
CONFIG_DEBUG_ASSERTIONS=y
CONFIG_DEBUG_FEATURES=y
CONFIG_DEBUG_FULLOPT=y
CONFIG_DEBUG_SYMBOLS=y
